version: '3.8'

services:
  backend:
    build: ./server
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - JWT_ALG=HS256
      - JWT_SECRET=your-super-secret-key-change-this
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/postgres
      - REDIS_URL=redis://redis:6379
      - ENVIRONMENT=LOCAL
      - SENTRY_DSN=https://fake-dsn@sentry.example.com/1
      - CORS_ORIGINS=http://localhost:5173
      - CORS_HEADERS=*
    depends_on:
      - db
      - redis

  db:
    container_name: app_db
    image: postgres:14.1
    ports:
      - "65432:5432"
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data/

  redis:
    container_name: app_redis
    image: redis:6.2-alpine
    ports:
      - "36379:6379"

volumes:
  postgres_data: 